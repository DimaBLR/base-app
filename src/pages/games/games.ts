import './styles.scss';
import { Component } from '../common/component';

export class Games extends Component {
  constructor(parent: HTMLElement) {
    super(parent, 'div', ['games-page'], 'Im games page');
  }
   
}